# Portfolio-demo artifact record

Build source: branch `polish/instascribe-live-onboarding-v1`, base
`88571e982c12148b002c4bf099964790ccbb99bb` (origin/main). Lockfile: `App/package-lock.json`,
lockfileVersion 3. Deliberate direct dev-dependency additions on this branch:
`@playwright/test` (browser suite) and `@axe-core/playwright` (accessibility scans) —
nothing else. Build command: `npm run build:portfolio-demo`
(`tsc -b && vite build --config vite.portfolio-demo.config.ts`; Vite 8, no source maps,
dedicated entry `portfolio-demo.html` → `src/portfolio-demo/main.tsx`). Verification:
`npm run verify:portfolio-demo` (forbidden-string audit, ≤ 21 MB budget, SHA-256 manifests);
`npm run test:portfolio-demo` (Playwright against the built bundle).

## Retained assets and provenance

SHA-256 for every fixture: `FIXTURES.sha256` (verified on every
`verify:portfolio-demo` run). Deploy manifest: `DEPLOY_MANIFEST.sha256` (regenerated per
build).

| Asset | Source | Provenance |
| --- | --- | --- |
| `App/public/videos/sintel-blender-cc.mp4` (8,940,006 B) | already in repo | ~120 s Sintel excerpt, © Blender Foundation, CC BY 3.0 (container tags carry the archive.org title + license URL); byte-identical to the handoff ZIP copy |
| `App/public/data/sintel-blender-cc/*.json` (6 files) | already in repo | pipeline outputs for the excerpt; byte-identical to the ZIP copies |
| `App/public/data/sintel-blender-cc/poster.{jpg,avif}` | already in repo | frames derived from the excerpt by the pipeline |
| `App/public/data/sintel-blender-cc/export.mp4` (8,534,135 B) | already in repo | pre-rendered described example (video stream identical to source; audio replaced with AAC mono 24 kHz AD mix) |
| `App/portfolio-demo-assets/data/sintel-blender-cc/tts/scene_{1..9}_onyx.mp3` (2,976,000 B total) | imported from the supplied handoff ZIP (audited; hashes match the ZIP audit record) | narration of each scene's ORIGINAL caption, OpenAI `tts-1-hd`, voice Onyx, per the handoff; **the exact generation commit is unproven** — treated as supplied material, not reproducible output; NOT regenerated (no API call was made) |
| `App/portfolio-demo-assets/data/sintel-blender-cc/{captions,described-captions}.vtt` | generated by `App/scripts/gen-demo-captions.mjs` | deterministically derived from the committed `transcript.json` / `scenes.json`; narration cues start at `scene.start + 0.25 s` (the export mux offset) and run for the **measured** ffprobe duration of each committed per-line clip — the closest measured proxy for the baked mix |

Deliberately **not** shipped: the ZIP's other 27 narration clips (nova/alloy/shimmer), its
compiled site, `HANDOFF.md`, and `tools/bake_tts.py` (never executed — it embeds another
user's absolute path and can start billable TTS jobs). The portfolio build also prunes the
shared-public files the demo never references — `demo/silence.mp3`, `icons.svg`,
`poster.avif`, `system_info.json` (internal pipeline metadata) and `audio_events.json`
(coarse pipeline audio map; the committed transcript is the demo's dialogue authority) —
so no unused application asset ships.

## ffprobe (retained media)

- `videos/sintel-blender-cc.mp4` — 120.000 s, h264 High L2.1 (avc1.640015) 564×240 @ 24 fps
  yuv420p, 460,969 b/s; AAC-LC stereo 48 kHz 129,578 b/s; overall 596 kb/s.
- `data/sintel-blender-cc/export.mp4` — 120.000 s, identical video stream; audio AAC-LC
  **mono 24 kHz** 103,398 b/s (the described mix); overall 569 kb/s.
- `tts/scene_{1..9}_onyx.mp3` — MP3 mono 24 kHz 160 kb/s CBR; durations (s): 20.688,
  17.112, 9.744, 14.856, 15.552, 15.144, 13.416, 20.424, 21.864.

## Size budget (measured at delivery)

- Deploy package total: **20.03 MB** of the 21 MB target (30 files; see
  `DEPLOY_MANIFEST.sha256` — the manifest lives only in docs/, so the deploy directory
  contains no unmanifested file).
- Intro payload before "Start now": HTML 0.81 kB + CSS 60.6 kB (11.9 kB gz) + JS 286.0 kB
  (91.2 kB gz) + 3 Geist woff2 (58.4 kB) — measured in-browser at **~161 KB transferred**;
  zero JSON or media requests (enforced by e2e).
- Deferred: 4 fixture JSONs (`scenes`, `transcript` — the dialogue authority —
  `ad_placement_gaps`, `entities`) + source video (poster first) load after Start now;
  each Onyx clip loads on its listen click; `export.mp4` loads only at the LISTEN step
  (enforced by e2e).
